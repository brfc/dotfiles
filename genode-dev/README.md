# Requires:
	- GENODE_DEV_DIR


Create a installation that adds alias to shell environment:
	- imx8_dev alias
	- linux_dev alias

These should be able to source the environment and execute it as is, from anyplace.
